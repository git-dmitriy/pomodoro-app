export interface ITheme {
  colors: {
    focus: string;
    break: string;
    rest: string;
    standby: string;
  };

  media: {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
    '2xl': string;
  };
}
